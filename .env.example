V0_API_KEY="your_v0_api_key" # Get one here: https://v0.dev/chat/settings/keys

# Debug Configuration
NEXT_PUBLIC_DEBUG_ENABLED="true" # Enable debug panel (default: true in development)
NEXT_PUBLIC_DEBUG_POSITION="right" # Debug panel position: 'left' or 'right' (default: right)

# Polling Configuration
NEXT_PUBLIC_POLLING_FREQUENCY="3000" # Chat status polling interval in milliseconds (default: 3000)